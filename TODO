Complete: TODOS
ADD notification to user when an event happened
ADD functionality for delete the user to a project
FINISH create task in backlog - Improve user input validation
add regex validation in backlog create task - title